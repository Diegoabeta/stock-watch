import { theme } from "@/src/styles/theme";
import styled from "styled-components/native";

export const NoStocksText = styled.Text`
  text-align: center;
  margin-top: 50px;
  color: ${theme.colors.textSecondary};
`;
